name: Build Dairy Farm APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            git \
            zip \
            unzip \
            openjdk-17-jdk \
            autoconf \
            automake \
            autopoint \
            libtool \
            pkg-config \
            zlib1g-dev \
            libncurses5-dev \
            libncursesw5-dev \
            cmake \
            libffi-dev \
            libssl-dev \
            build-essential

      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          python -m pip install setuptools wheel
          python -m pip install "cython==0.29.34"
          python -m pip install buildozer==1.5.0

      - name: Check Python
        run: |
          python --version
          python -m pip --version
          buildozer --version

      - name: Create Buildozer spec
        run: |
          buildozer init

      - name: Configure Buildozer
        run: |
          sed -i 's/^title = .*/title = Dairy Farm/' buildozer.spec
          sed -i 's/^package.name = .*/package.name = dairyfarm/' buildozer.spec
          sed -i 's/^package.domain = .*/package.domain = org.dairyfarm/' buildozer.spec

          sed -i 's/^source.include_exts = .*/source.include_exts = py,png,jpg,jpeg,kv,json,db,sqlite3/' buildozer.spec

          sed -i 's/^requirements = .*/requirements = python3,kivy/' buildozer.spec

          sed -i 's/^orientation = .*/orientation = portrait/' buildozer.spec

          sed -i 's/^fullscreen = .*/fullscreen = 0/' buildozer.spec

          sed -i 's/^android.api = .*/android.api = 36/' buildozer.spec
          sed -i 's/^android.minapi = .*/android.minapi = 24/' buildozer.spec

          sed -i 's/^android.archs = .*/android.archs = arm64-v8a/' buildozer.spec

          # Use stable python-for-android branch
          sed -i 's/^#p4a.branch = .*/p4a.branch = master/' buildozer.spec

          echo "===== BUILD CONFIG ====="
          grep -E 'title =|package.name =|package.domain =|requirements =|android.api =|android.minapi =|android.archs =|p4a.branch =' buildozer.spec

      - name: Clean previous Buildozer files
        run: |
          rm -rf .buildozer
          rm -rf bin

      - name: Build APK
        run: |
          yes | buildozer -v android debug

      - name: Check APK
        run: |
          echo "===== APK FILES ====="
          find . -type f -name "*.apk" -print

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: dairy-farm-apk
          path: bin/*.apk
          if-no-files-found: error
